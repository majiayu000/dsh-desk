{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updates",
  "description": "Minimal permissions for the signed desktop update window.",
  "windows": ["updates"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "desktop-updates"
  ]
}
