{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "bootstrap",
  "description": "Minimal permissions for the local startup and recovery page.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
